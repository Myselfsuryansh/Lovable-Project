spring:
  application:
    name: lovable-clone

  datasource:
    url: jdbc:postgresql://localhost:5432/lovable?useSSL=false
    username: suryansh
    password: "5487"

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

      javax:
        persistence:
          validation:
            mode: none

jwt:
  secret-key: 12345678901234567890123456789012345678901234567890
server:
  port: 8080